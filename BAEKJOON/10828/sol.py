import sys
sys.stdin = open("input.txt", "r")

N = int(input())
def command_def(command, num):
    if command = 
for _ in range(N):
    try:
        command, num = list(input().split())
        return command_def(command, num)
    else:
        command = str(input())
        pass
